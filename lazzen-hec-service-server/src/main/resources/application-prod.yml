#
# -------------------------------------------------
# SIPA BOOT CONFIGURATION ( 一般外挂配置, 用不到这个 )
# -------------------------------------------------
#
sipa.boot:
  #
  # -------------------------------------------
  # DYNAMIC DATA SOURCE CONFIGURATION
  # -------------------------------------------
  #
  datasource:
    enabled: true
    names:
      - mysql
    dynamics:
      mysql:
        datasource:
          names: master,slave0
          master:
            type: com.alibaba.druid.pool.DruidDataSource
            driver-class-name: com.mysql.cj.jdbc.Driver
            url: jdbc:mysql://xx:3306/lite_store?useUnicode=true&characterEncoding=utf8&useSSL=false&tinyInt1isBit=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
            username: root
            password: xx
          slave0:
            type: com.alibaba.druid.pool.DruidDataSource
            driver-class-name: com.mysql.cj.jdbc.Driver
            url: jdbc:mysql://xx:3306/lite_store?useUnicode=true&characterEncoding=utf8&useSSL=false&tinyInt1isBit=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
            username: root
            password: xx
        masterslave:
          load-balance-algorithm-type: round_robin
          name: ms
          master-data-source-name: master
          slave-data-source-names: slave0
